
function addNumbers(obj){
    

    // const len= matrix.length;

    
    $('td').each(function (index, value) {
        // console.log('div' + index + ':' + $(this).attr('id'));
        // console.log (value); 
        $( this ).append( "<div>1</div>" )
        
      
    })

      // for (var i=0; i<len; i++) {
        //     for (var j=0; j<len; j++) {  
        //     });
            
        // }
    
    
}